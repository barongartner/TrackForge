bin/
obj/
*.user
*.suo
.vs/
.vscode/
publish/
artifacts/
installer/publish/
installer/out/
*.wixpdb

*.log
config.json

# macOS build output. TrackForge.icns is committed - it only changes when the
# mark does, and regenerating it needs Tools/makeicon.sh.
macOS/.build/
macOS/.build-tests/
macOS/.build-tools/
macOS/.build-dmg/
macOS/dist/
macOS/TrackForge.app/

Thumbs.db
desktop.ini
.DS_Store
